/*
 * GoogleTranslate Messages
 *
 * This contains all the text for the GoogleTranslate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GoogleTranslate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleTranslate container!',
  },
});
