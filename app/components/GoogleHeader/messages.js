/*
 * GoogleHeader Messages
 *
 * This contains all the text for the GoogleHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GoogleHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleHeader component!',
  },
});
